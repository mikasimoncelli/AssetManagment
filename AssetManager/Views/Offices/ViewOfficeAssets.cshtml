@model IEnumerable<AssetManager.Models.Asset>
<body>


    <div class="container-wrapper">
        <div class="form-container">
            <h3 class="text-center mt-4 mb-4 display-4">@ViewBag.Office.OfficeName Assets</h3>
    <h5 class="text-center display-6">@ViewBag.Office.Location</h5>

    <div class="row justify-content-center mb-3">
        <div class="col-md-10">
                                    <button type="button" style="width: 150px;" onclick="window.location.href='@Url.Action("Index", "Offices")'" class="btn btn-secondary">Back to Offices</button>
                                    <br />
<br />
            <h6 class="form-label">Filter by Asset Type:</h6>
            <select id="assetTypeDropdown" class="form-control mb-3" onchange="filterAssetType()">
                <option value="">All Asset Types</option>
                @foreach (var type in ViewBag.AssetTypes as List<string>)
                {
                    <option value="@type">@type</option>
                }
            </select>

            <div class="input-group mb-3">
                <input type="text" id="searchInput" class="form-control" placeholder="Search assets..." onkeyup="applyGlobalSearch()" style="margin-right: 20px;" />
                <button type="button" class="btn btn-primary" onclick="resetAllFilters()">Reset Search</button>
            </div>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-10">
            <div class="table-responsive">
                @if (Model != null && Model.Any())
                {
                    @foreach (var equipmentGroup in Model.GroupBy(c => c.EquipmentType))
                    {
                        <div class="equipment-type-section" data-type="@equipmentGroup.Key">
                            <h4 class="text-center mt-4 mb-3" style="color: #343a40;">@equipmentGroup.Key</h4>

                            <table class="table table-bordered text-center table-sm asset-table">
                                <thead>
                                    <tr style="background-color: #0d6efd; color: #fff;">
                                        <th>Asset ID</th>
                                        <th>Asset Type</th>
                                        <th>Description</th>
                                        <th>AssetNumber</th>
                                        <th>SerialNumber</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var asset in equipmentGroup)
                                    {
                                        <tr onclick="window.location.href='@Url.Action("EditAsset", "Assets", new { id = asset.AssetID })'">
                                            <td>@asset.AssetID</td>
                                            <td>@asset.EquipmentType</td>
                                            <td>@asset.Description</td>
                                            <td>@asset.AssetNumber</td>
                                            <td>@asset.SerialNumber</td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                }
                else
                {
                    <p class="text-center">No assets available in this office.</p>
                }
            </div>
        </div>
    </div>
</div>
</body>
<script>
    function filterAssetType() {
        const selectedType = document.getElementById("assetTypeDropdown").value.toLowerCase();
        document.querySelectorAll(".equipment-type-section").forEach(section => {
            const type = section.getAttribute("data-type").toLowerCase();
            section.style.display = selectedType && type !== selectedType ? "none" : "block";
        });
    }

    function applyGlobalSearch() {
        const searchValue = document.getElementById('searchInput').value.toLowerCase();
        document.querySelectorAll(".equipment-type-section").forEach(section => {
            const rows = section.querySelectorAll("tbody tr");
            let rowMatchCount = 0;

            rows.forEach(row => {
                const rowText = Array.from(row.cells).map(cell => cell.textContent.toLowerCase()).join(" ");
                const isVisible = rowText.includes(searchValue);
                row.style.display = isVisible ? "" : "none";
                if (isVisible) rowMatchCount++;
            });

            section.style.display = rowMatchCount > 0 ? "block" : "none";
        });
    }

    function resetAllFilters() {
        document.getElementById('searchInput').value = '';
        document.getElementById('assetTypeDropdown').value = '';
        document.querySelectorAll(".equipment-type-section").forEach(section => {
            section.style.display = "block";
            section.querySelectorAll("tbody tr").forEach(row => row.style.display = "");
        });
    }
</script>


<style>
    .container-wrapper {
        display: flex;
        justify-content: center;
        width: 100%;
        padding: 20px;
    }

    .form-container {
        width: 2100px;
        padding: 50px;
        border: 2px solid #ddd;
        border-radius: 8px;
        background-color: #fdfdfd;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.15);
    }

    .container-fluid {
        background-color: #f8f9fa;
    }

    .asset-table {
        width: 100%;
        border: solid 1px #ddd;
    }

    th, td {
        cursor: pointer;
        padding: 8px;
    }

        th:hover, tr:hover {
            background-color: #dcdcdc;
        }

    .equipment-type-section {
        margin-bottom: 30px;
        padding: 15px;
        border: 1px solid #ddd;
        border-radius: 5px;
    }
</style>
