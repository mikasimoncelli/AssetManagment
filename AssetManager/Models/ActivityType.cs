namespace AssetManager.Models
{
    public enum ActivityType
    {
        CreateAsset,
        EditAsset,
        CreateLoan,
        EditLoan,
        CreateDamage,
        EditDamage,
        CreateDisposal,
        EditDisposal,
        LoanReturned
    }
}
